# Cloudflare Deployment Configuration

# Frontend Environment Variables (Vite)
# Set this to your deployed Cloudflare Worker URL
VITE_API_URL=https://career-constellation-api.your-subdomain.workers.dev

# Backend Environment Variables (Worker)
# These are set via wrangler secret put, not in .env
# - GEMINI_API_KEY: Your Google Gemini API key for RAG chat

# Development
# During local development, the Vite proxy handles /api routes
# VITE_API_URL is not needed in development
